package main

import "fmt"

func main() {
	fmt.Println("Hello World")
}

// package - multiple files of go kept in logical group
// main() is the entry point
// fmt -> std.lib package used for formatting


// to build a file -> go build {filename}.go

// to run a file -> ./{filename}

// direct build and run -> go run {filename}.go